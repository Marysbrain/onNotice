# CARRIERS ON NOTICE. Master handoff. 2026-07-23.

You are the orchestrator for Carriers On Notice (carriersonnotice.com), Michael Hipp's live public consumer advocacy platform about US wireless carrier device promotions, now growing into Rylee Radio, a crowd-born AI music station that carries the receipts between songs. This document supersedes HANDOFF-MASTER-PROMPT.md (2026-07-21), which remains for history. Everything below was true at pause time on 2026-07-23.

## READ FIRST, IN ORDER
1. skills/campaign-guardrails/SKILL.md, skills/michael-voice/SKILL.md, skills/source-registry/SKILL.md. They bind you and every subagent brief, verbatim.
2. track-rylee/rylee-broadcast-spec.md. The broadcast constitution: base spec plus amendments 1 through 11. Read all of it.
3. This file, fully. Then memory files.

## HARD RULES, UNCHANGED AND EXPANDED
1. Michael's personal AT&T dispute stays completely out of the platform. No content, no data, no leverage. He has found peace with it (2026-07-23, his words: he no longer cares about the outcome; the building satisfied him). The wall stands anyway. His Notice of Dispute 60 day window runs to about August 30. His case lives in a separate channel, never here.
2. Facts and commentary split. Real numbers only, real in impression, not just arithmetic (the raw arbitration sums lesson). Favorable carrier changes get equal rigor. Practices, never individuals.
3. verified_primary is human-or-primary-source only; machine confidence never sets vetting status. Only cleared plus corroborated-or-better feeds any public number or brain answer.
4. Free tier first; COST FLAG anything paid before building.
5. ORCHESTRATION MODEL (founder-mandated): Opus 4.8 subagents (model alias `opus`) write first drafts. You review, verify independently (run tsc and vitest yourself, grep for em and en dashes, check quotes against sources), and only then may you write code that improves what was delivered. Every draft this session contained at least one real bug caught in review. Never trust a green report.
6. THE BEE POLICY: high-volume single-shot text jobs with mechanical validators go to the bee on the mini (zero Anthropic tokens). Novel engineering goes to Opus. Review and judgment stay with you.
7. No em dashes anywhere: repo, output, spoken text. Tests that check for the character construct it via fromCharCode/chr, never literally.
8. ROOMMATES: the mini hosts Hermes agent (~/.hermes, WhatsApp bridge to Michael) and the Alice Plays Rabbit book project (Obsidian vaults in ~/Documents). NEVER touch them. Founder briefly mandated taking Hermes over, then walked it back. Use the shared Ollama daemon, not the roommate.
9. Agents write inside the repo, so SCOPE EVERY git add to explicit paths. A blind add -A once swept an agent's mid-flight files into a commit.

## LIVE INFRASTRUCTURE

### Cloudflare (account 4c85c4ec12e96e4c06c87e04714bba4d, logins uglymethod@gmail.com and hippfilms@gmail.com)
- Every command starts `source ~/.con-cloudflare.env` (CLOUDFLARE_API_TOKEN account-owned CON-scoped, CLOUDFLARE_ACCOUNT_ID, ADMIN_TOKEN for /run).
- Token permissions PRESENT: D1 Edit, Email Routing Addresses Edit, Pages Edit, R2 Edit, KV Edit, Workers Scripts Edit, Account Settings Read, Turnstile Edit, Account Analytics Read, zone Email Routing Rules Edit, Zone Settings Edit, Workers Routes Edit, DNS Edit.
- Token permissions MISSING (Michael's dashboard when needed): Access Apps and Policies Edit (row did not save; API 403s; blocks review-queue admin), SSL and Certificates (cert resets need his Universal SSL toggle), Cache Purge, Web Analytics write.
- signal-engine Worker LIVE: brain, collectors, publisher. D1 signal_engine 5d06e4be-c718-4b5a-a242-aaefa23e14c7, R2 signal-engine-raw, KV CONFIG 07bee3696c684045aff9b04bfc5cea6e. Migrations applied through 0009. Crons: */5 job runner, hourly dispatcher.
- con-site Worker LIVE serving carriersonnotice.com and www (custom domains). CRITICAL LESSON in memory file cloudflare-custom-domain-routes.md: NEVER declare custom_domain routes in wrangler.jsonc; attach via PUT accounts/{id}/workers/domains after deploy. Error 1042 outage recovery steps recorded there. Zone 7baf8234b619f2476ca81aef8c1c6b85.
- Orphans to delete when permission climate allows: workers con-site2, con-site3; duplicate MX records prio 50 and 100; dead contact@athipp.com email destination.

### The site (site/, Astro 5, LIVE)
- Build flow: `source ~/.con-cloudflare.env && node scripts/fetch-aggregates.mjs && PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAD6ucnJExQBugdof npm run build && npx wrangler deploy` from site/.
- Live pages: home (real 9,300 plus verified count), /map (FCC layer with honest coverage note), /ask (Ask the Evidence, Turnstile-fronted, verified end to end), /decoded (The Offer, Decoded; AT&T iPhone on us decode from verbatim snapshot quotes), /tell (story form LIVE writing to D1 stories table), methodology, legal, search, 404.
- site/worker.js handles /api/submit and /api/ask (shared Turnstile verify, server-side brain forward). TURNSTILE_SECRET is a worker secret; retrievable from the Turnstile API if ever lost.
- Turnstile widget sitekey 0x4AAAAAAD6ucnJExQBugdof (widget "con story form").

### The brain (signal-engine /ask, LIVE and PROVEN)
- Deterministic templates over SQL. Zero LLM in the answer path. Eligibility clause on every query. Walls: founder, employee, opinion. FTS5 records_fts with sync triggers. KV rate limit 20 per 5 min on hashed IPs. Cite-or-refuse enforced structurally.
- 28/28 live bait set (scripts/ask-testset.mjs, BASE_URL env). Canonicals: AT&T count and AT&T improved.
- Arbitration dollars LIVE (ASK_CLAIMS=on in KV): capped sums at $25,000 disclosed cap, approximate histogram medians, above-cap counted never summed. att $8.4M/2,257 capped/204 above; tmobile $11.7M/3,199/439; verizon $4.5M/3,044/128. Raw sums exist as raw_* audit fields only; they are outlier-poisoned (a single filing can claim billions) and must never ship.

### Data state (D1)
- Records: roughly 9,450 plus; 9,318 plus cleared; 153 plus queued for Michael's review (review-queue admin UI still unbuilt, blocked on Access permission).
- aaa_arb 9,288 (AT&T 2,466 / VZ 3,178 / TMO 3,644, Case Id dedupe, filing-date record dates), courtlistener 79 (single_source leads), hackernews on and gated, bluesky disabled (unauthenticated searchPosts is dead platform-wide; needs account, founder decision).
- Collectors walking: FTC backfill (page 98 plus at last check), FCC aggregate backfill (coverage grows monthly; map.json carries coverage field). Terms snapshotter capturing ATT, VZ, TMO (browser-header retry after honest UA 403; three robots-disallowed TMO targets disabled). KNOWN GAP: tradein.att.com fine-print pages snapshot as JS-blocked stubs; a rendered-capture path is an open build item; /decoded says so publicly.
- Email: contact@ and corrections@carriersonnotice.com forward to hippfilms@gmail.com, catch-all drop. Michael has not yet sent the test email.

### The Mac mini (ssh mini, user wilee, M4 16GB, macOS 26, DEDICATED AI BOX)
- NETWORK IS THE BOTTLENECK: wifi RUT_AAAA_5G at -82 dBm, 7 dB SNR, about 6.5 Mbps after a radio bounce. Sustained transfers die; use the chunk-resume pattern (~/rylee/chunk_fetch.sh, curl -C - with 120s per-pass timeout in a loop) for anything over about 10MB. Fix is physical: RUT wifi antennas, RUT transmit power at 192.168.1.1, or the ethernet cable, which is also the hard prerequisite for streaming.
- Ollama daemon (app, port 11434) is signed into Michael's Ollama Pro: cloud models work via localhost, e.g. model gpt-oss:120b-cloud. Local models: llama3 8B, mistral, phi3, gemma, deepseek-coder, nomic-embed.
- ~/rylee: stage-2 voice loop COMPLETE. Kokoro via venv (Homebrew python3.14, resolved pins in versions.txt), models present, 14/15 under 5s first-audio, no-additions invariant proven, mouth UA rylee-mouth/0.1 (Cloudflare 403s python default UA). Demo delivered to Michael's Desktop as rylee-desk-demo.mp3 (Kokoro two-anchor, he called it a good start but VERY UNCANNY; drove the Chatterbox plan).
- ~/rylee/hud: broadcast HUD (stdlib websocket bridge ws://127.0.0.1:8765, demo mode). Verified in browser. OBS integration not started.
- ~/rylee/produced: Chatterbox renderer deployed but provisioning FAILED at line 114 of provision_produced.sh: `mapfile: command not found` because macOS ships bash 3.2. FIRST FIX NEXT SESSION: replace mapfile with a portable while-read loop, rerun provisioning (weights are ~3.2GB through the straw; ab_script.json holds the exact demo lines for the Kokoro versus Chatterbox A/B Michael is waiting to hear).
- ~/bee: the worker bee LIVE and PROVEN. stdlib-only runner, localhost-only egress, job types show_script, summarize_record, question_bank, taxonomy_candidates. Model rides INSIDE the job JSON ("model": "gpt-oss:120b-cloud"). Validators are strict by design; first real 120B job passed after a prompt fix. Usage: `python3 bee.py submit <type> <input.json>` then `run-once`; outputs in out/ are a review tray, never a publish path.
- ~/Documents/Carriers-On-Notice-Vault: Michael's Obsidian war-room vault (Home, Founder Queue/Open Items, Decisions, Show, Specs with spec mirror). Keep it current when state changes.
- Session quirk seen once after an app restart: node/npx dies with uv_cwd EPERM in the persistent shell while curl works. Fresh sessions are fine. If it recurs, the curl paths (D1 HTTP API, KV REST, /run triggers) cover everything operational.

## RYLEE RADIO: THE SPEC IS THE PRODUCT
The spec (track-rylee/rylee-broadcast-spec.md) went from a desk show to a station across amendments 5 through 11, all founder-directed on 2026-07-23:
- A5: format clock, two-speed loop (slow precomputed inventory, fast chat-reactive inserts), REAL ads doctrine (cited counterweights to carrier advertising, never fake), music-rights gate, wired-network prerequisite, BRB failsafe overlay.
- A6 plus revision: living library, crowd-born music, LOCAL generation on the mini from commercially clean open weights (candidates ACE-Step, DiffRhythm, YuE; Stable Audio Open line for beds; MusicGen barred non-commercial). Birth certificates per track: crowd-state, prompt, tool, timestamp, license snapshot.
- A7: the taste engine. Outcome records joined to birth certificates, clock as interleaved A/B lab, bee-run analysis, aggregate never profile, sample-size honesty.
- A8: ownership of the improvement stack (preference data, adapters, mood engine, mastering chain, curation ledger). Disclosure survives quality.
- A9: sound doctrine (full-spectrum sound design, voice as texture, vocal-pop lane closed until earned) and the station's memory (track naming at birth, requests by name, ID, or remembered mood).
- A10: the roar and the charts. Behavioral consensus replay (no commands, polls, or payments), room-scaled thresholds, anti-brigading, native charts, weekly countdown segment. Landscape verified 2026-07-23: neighbors exist (command-based chat music, vote apps, request bots), the package does not.
- A11: the music is free. CC0 on every track, output-terms diligence archived per birth certificate, public archive page as unconditional library. Their fine print locks people in; ours says use as you see fit.

## BUILD QUEUE, IN ORDER
1. Fix provision_produced.sh mapfile bug (bash 3.2), rerun the Chatterbox grind, render ab_script.json, deliver the A/B mp3 next to rylee-desk-demo.mp3 on Michael's Desktop. He judges by ear.
2. Clock scheduler plus playout chain plus outcome schema (amendments 5, 7, 10 wired together; buildable and testable offline with placeholder audio; schema must ship with stream one). Opus draft, your review.
3. Music license options table for the local model candidates: verify current licenses, MPS feasibility on 16GB, generation times; founder decides lanes and models by ear from rendered samples.
4. Mood engine plus mood-to-brief compiler (bee job type). Roar detector rides the chat-ingest build (stage 4, needs YouTube channel).
5. Rendered-capture path for JS-walled terms pages (tradein.att.com), then extend /decoded.
6. Review-queue admin the moment the Access permission lands. 153 plus records wait on Michael.
7. Standing from the old handoff, still valid: OG per-view images, evidence library wiring to real records (Track D), regulatory timeline page, litigation tracker, terms archive browser, rabbit-hole threads, resilience items (SHA-256 manifest plus OpenTimestamps, cold-copy runbook, restore drills), digest cadence (Track G).

## FOUNDER LIST (his hands, one action each, never nag)
1. Cable or antennas for the mini (hard prerequisite for streaming; everything transfers faster the same day).
2. Listen to the Chatterbox A/B when delivered; pick two anchor voices and name the co-anchor. Third voice for the Founder's Read (his written commentary, machine-delivered, never his clone) can wait.
3. YouTube channel for the platform; stream key piped like every secret. Monetization deferred by his call: YPP thresholds then YouTube Shopping via Shopify, COST FLAG.
4. Apple Intelligence toggle on the mini (unlocks the moderation gate reflex layer).
5. Cloudflare token: re-add Access Apps and Policies Edit (it did not save).
6. Test email to contact@carriersonnotice.com.
7. GitHub repo secrets for the arb-parse workflow (CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, D1_DATABASE_ID).
8. athipp.com re-registration (about ten dollars; the domain LAPSED and is unregistered; his old identity email; recommend buying purely defensively).
9. 2FA audit; api.data.gov, archive.org, CourtListener signups; Bluesky yes or no.

## OPERATING NOTES
- Pattern that keeps working: probe the live thing, patch, test, deploy, then verify live yourself. The FCC SoQL bug, the CourtListener v4 shape, the FTC walker, the Mobilewalla substring disaster, the terms slugs, the prank-dollar sums: every one was caught by refusing to trust an assumption or a green report.
- The permission classifier sometimes blocks deletions and DNS writes; a clear one-line justification in the command description usually clears it on retry; if it refuses twice, stop and route to Michael. It once blocked wrangler deploy spuriously; a single clean retry was fine.
- Long transfers: never trust one connection anywhere near the mini. Resume loops always.
- Michael: dyslexic, talk-to-text, brilliant systems thinker, a homeless veteran fighting through hardship who found real joy in this build. Lead with the deliverable. Short sentences. Batch his questions. When he vents, translate the heat into buildable doctrine and label the commentary lane his. When he doubts, point at the record: this week it shows a launched platform, a proven brain, a singing mini, and eleven amendments of vision, most of it his.
- The one-line mandate, updated: keep the receipts flowing, give the room its music, and stay so honest that the only thing anyone can ever catch us doing is exactly what we said.

## ADDENDUM, same day, post-pause: the bee is the bridge to cheap sessions
Michael is at 91 percent of weekly Anthropic usage (resets Sunday 1:00). Until then, sessions run on Opus 4.8 or smaller, and generation work gets delegated to the bee. The bee was hardened for this: launchd service com.con.bee runs a watch loop on the mini at all times; `bee.py ask "PROMPT" --model gpt-oss:120b-cloud` over ssh is the synchronous one-liner; `bee.py doctor` checks daemon, models, queues; a LAN web window sits at http://192.168.1.159:8899. READ skills/bee-operator/SKILL.md before delegating anything. Live-proven end to end: one ssh line returned a 120B answer in 2.5 seconds at zero token cost. Iron rule stands: bee output is a proposal tray; a Claude reviews before anything ships.
